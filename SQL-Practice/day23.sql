-- ================================================
-- Day 23 - SQL Learning Notes
-- Topic: SQL Revision - Exam Period
-- ================================================

-- ------------------------------------------------
-- NOTE
-- ------------------------------------------------

-- Semester exams are currently ongoing
-- Doing daily revision to stay consistent
-- Maintaining GitHub streak during exam period
-- Full SQL projects will start after exam

-- ------------------------------------------------
-- WHAT I REVISED TODAY
-- ------------------------------------------------

-- Revised all SQL Commands
--   DDL - CREATE DROP ALTER
--   DML - INSERT UPDATE DELETE
--   DQL - SELECT
-- Revised all Clauses
--   SELECT FROM WHERE GROUP BY
--   HAVING ORDER BY LIMIT OFFSET
-- Revised all Operators
--   AND OR NOT IN BETWEEN LIKE
--   IS NULL IS NOT NULL
-- Revised all Built-in Functions
--   Aggregate String Logical
--   Date Time Numeric Window
-- Revised JOINS all 4 types
-- Revised Subqueries all 3 types
-- Revised CTE and Window Functions
-- Revised Normalization 1NF 2NF 3NF

-- ------------------------------------------------
-- QUICK REVISION NOTES
-- ------------------------------------------------

-- SQL COMMANDS
-- DDL = CREATE DROP ALTER
-- DML = INSERT UPDATE DELETE
-- DQL = SELECT

-- CLAUSES ORDER
-- SELECT FROM WHERE
-- GROUP BY HAVING ORDER BY
-- LIMIT OFFSET

-- OPERATORS
-- AND OR NOT
-- IN BETWEEN LIKE
-- IS NULL IS NOT NULL

-- JOINS
-- INNER JOIN = matching rows only
-- LEFT JOIN  = all left matching right
-- RIGHT JOIN = all right matching left
-- FULL JOIN  = all rows both tables

-- SUBQUERIES
-- Single Row  = one value  use = > 
-- Multi Row   = many values use IN
-- Correlated  = inside SELECT clause

-- CTE
-- WITH name AS (query)
-- SELECT * FROM name
-- Temporary table during query only

-- WINDOW FUNCTIONS
-- ROW_NUMBER = unique number each row
-- RANK       = rank with gaps for ties
-- DENSE_RANK = rank without gaps
-- NTILE      = divide into n groups
-- LAG        = value from previous row
-- LEAD       = value from next row
-- PARTITION BY = resets per group

-- NORMALIZATION
-- 1NF = atomic values no repeating groups
-- 2NF = no partial key dependencies
-- 3NF = no transitive dependencies

-- AGGREGATE FUNCTIONS
-- COUNT SUM AVG MAX MIN
-- COUNT DISTINCT

-- STRING FUNCTIONS
-- UPPER LOWER LENGTH
-- LEFT RIGHT SUBSTRING
-- TRIM REVERSE

-- LOGICAL FUNCTIONS
-- IF IFNULL NULLIF
-- COALESCE CASE WHEN

-- DATE TIME FUNCTIONS
-- CURRENT_DATE NOW DAY
-- MONTH YEAR HOUR MINUTE
-- DATE_ADD DATE_SUB DATEDIFF

-- NUMERIC FUNCTIONS
-- ABS CEIL FLOOR
-- MOD ROUND POWER

-- ------------------------------------------------
-- SQL PROJECTS PLANNED AFTER EXAMS
-- ------------------------------------------------

-- Project 1 = EDA with SQL
-- Project 2 = Data Cleaning with SQL
-- Project 3 = SQL Project
-- Project 4 = SQL with Dashboard
-- Project 5 = E-Commerce Own Project

-- ------------------------------------------------
-- SUMMARY - WHAT I DID TODAY
-- ------------------------------------------------

-- Quick but complete revision done
-- All SQL topics covered in revision
-- Consistent daily practice maintained
-- GitHub streak continuing even in exams
-- Ready to start projects after exams

-- ------------------------------------------------
-- TOMORROW I WILL
-- ------------------------------------------------

-- Continue exam preparation
-- Do quick SQL revision
-- After exams projects will begin!